{
  "extends": "./tsconfig.json",
  // "scripts" lives outside src; leaving it in would lift the compiler's root
  // directory and emit dist/src/main.js instead of dist/main.js.
  "exclude": ["node_modules", "test", "dist", "scripts", "**/*spec.ts"]
}
